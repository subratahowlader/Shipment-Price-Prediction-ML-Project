from setuptools import find_packages, setup

setup(
    name="shipment",
    version="0.0.1",
    author="Subrata",
    author_email="contact@mysubrata.com",
    packages=find_packages(),
    install_requires=[],
)